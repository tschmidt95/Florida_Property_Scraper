"""Legacy module placeholder.

This repo previously contained a "listing-provider" comps workflow. The current
mission is PA-only: all attributes and comps must be derived solely from County
Property Appraiser (PA) extracted data.
"""

raise ImportError(
	"Legacy listing-based comps workflow removed. Use the PA-only commands: "
	"`pa-search` and `pa-comps`."
)
